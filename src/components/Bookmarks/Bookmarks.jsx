

const Bookmarks = ({bookmarks}) => {
  return (
    <div className="w-1/3 bg-sky-200">
      <h2 className="text-3xl">this is bookmarks {bookmarks.length}</h2>
    </div>
  );
};

export default Bookmarks;